const mongoose = require('mongoose');
const sli

const brandSchema = new mongoose.Schema({
    name:{
        type:String,
        nique:true,
        minLength:3,
        maxLength:14, 
    },
    s
})